services:
  app:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: my_app

    working_dir: /app

    volumes:
      - .:/app

    command: xxxxx

    environment:
      PYTHONDONTWRITEBYTECODE: "1"
      PYTHONUNBUFFERED: "1"

    tty: true