[package]
name = "component_1"
version = "0.1.0"
edition = "2025"

[dependencies]
# Local
shared = { path = "../shared" }

# External managed in global Cargo.toml
serde = { workspace = true }
tokio = { workspace = true }

# External only in this crate
log = { version = "0.4"", features = ["full"] }