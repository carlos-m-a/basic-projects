# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]
### Added
- Integration of CI/CD pipelines in the `deploy/` directory.
- Implementation of the first core domain entities.

## [0.1.0] - 2026-05-08
### Added
- Initial project structure following Hexagonal Architecture.
- Modern Python environment setup using `uv`.
- Automated task management via `Makefile`.
- Professional configuration suite (`.editorconfig`, `.gitignore`, `.env.example`).
- Support for `src` layout for better package distribution.